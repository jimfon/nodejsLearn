'use strict';
//!!! UNUSED because it's different from java package! 

/*const {assert, bodyParser, childProcess, cookieParser, crypto, dns, domain, events, express, fs, http, https, multer, mysql, net, os, path, querystring, stream, url, util, zlib} = require('/src/main/nodejs/com.fengmu.tp/requireVar');


module.exports = class MyTool{
    constructor() {}
    readstr(filename, blocksize = 512) {
        let fin = fs.createReadableStream(filename);
        let tex = '';
        fin.on('readable', () => {
            let buff = fin.read(blocksize);
            tex+=buff;
        });
        
        fin.on('end', () => {
            console.log(tex);
        });
        return 4;
    }

};


if (require.main != undefined && require.main.filename != undefined && require.main.filename.endsWith('test.js')) {
    let readstr=new MyTool();
    readstr.readstr("D:\\~MSSETUP.T\\web\\nodejs\\tp\\.gitignore");
}
*/
